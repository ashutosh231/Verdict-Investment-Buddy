# Leave empty to use same-origin requests (Vite dev proxy forwards /api to
# the backend). In production, set this to your deployed backend URL, e.g.
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
